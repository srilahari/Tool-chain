
name: Colab-CI-Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "🔥 Colab Toolchain Connected Successfully for srilahari/Tool-chain!"
